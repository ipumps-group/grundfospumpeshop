// Middleware (proxy.ts) handles locale detection and internally rewrites / to the [locale] page.
// This component is never rendered in normal flow.
export default function RootPage() {
  return null
}